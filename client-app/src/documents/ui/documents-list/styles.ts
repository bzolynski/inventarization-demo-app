import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        textAlign: 'center',
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 10,
    },
    container: { overflow: 'visible', flex: 1 },
});

export default styles;
